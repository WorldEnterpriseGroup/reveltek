# checkpoints/ — immutable gate snapshots

No gate snapshots recorded yet. `checkpoint.json` is the current pointer/summary;
snapshots land here only when a gate is actually evaluated with evidence.

An empty directory means the corresponding gates are **unknown, not passed**.
Do not treat the absence of snapshots as acceptance.
